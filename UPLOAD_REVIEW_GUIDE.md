# AMC·수능 문제 업로드 및 검수 사용법

이 문서는 PDF·텍스트·문제 캡처 이미지를 인터랙티브 문제로 변환한 뒤, 인식 오류를 문제별로 수정하는 방법을 설명합니다.

## 작업 위치

- AMC: `/amc/admin` → `AI 인터랙티브 문제 자동 변환기`
- 수능: `/csat/admin` → `AI 인터랙티브 문제 자동 변환기`
- 로컬 실행: 프로젝트 폴더에서 `npm run dev` 실행 후 `http://localhost:3000` 접속

## 전체 PDF 또는 텍스트 파일 변환

1. 관리자 화면에서 AMC 등급 또는 수능 시험 유형을 선택합니다.
2. `전체 파일 불러오기`를 눌러 PDF, TXT, TeX, Markdown 또는 JSON 파일을 선택합니다.
3. PDF는 자동으로 텍스트를 추출하고 문제별로 분리합니다.
4. 화면 상단의 `인식 문항 수 / 예상 문항 수`와 `누락 번호`를 확인합니다.
   - AMC 예상 문항 수: 25문항
   - 수능 예상 문항 수: 30문항
5. 각 문제 카드에서 내용을 확인하고 `검수 완료`를 선택합니다.
6. 검수가 끝나면 `인터랙티브 시험 세트로 저장`을 누릅니다.

중요: 인식된 문항이 15개 또는 18개뿐이면 시스템은 없는 문제를 임의로 채우지 않습니다. 실제 인식 결과와 누락 번호를 그대로 표시합니다.

## 문제별 수정

각 문제의 검수 카드에서 다음 항목을 직접 타이핑할 수 있습니다.

- 단원
- 객관식/주관식 유형
- 배점
- 문제 내용
- 선택지 A~E(추가·삭제 가능)
- 정답
- 해설

`학생 화면 미리보기`를 열면 수식과 선택지가 실제 시험 화면에서 어떻게 보이는지 확인할 수 있습니다. 빨간 테두리와 `확인` 문구는 문제 내용 없음, 선택지 개수 오류, 빈 선택지, 정답 범위 오류를 뜻합니다.

## 누락 문제 직접 추가

1. 결과 상단의 누락 번호 목록을 확인합니다.
2. `+ 누락 문제 직접 추가`를 누릅니다.
3. 시스템이 가장 앞의 누락 번호로 빈 문제를 만듭니다.
4. 문제와 선택지를 타이핑하고 정답을 지정한 뒤 `검수 완료`를 선택합니다.

## 문제 한 개 또는 문제지 일부 캡처 OCR

1. 원본 PDF 뷰어나 사진 앱에서 한 문제 또는 필요한 부분만 잘라 PNG/JPG로 저장합니다.
2. `시작 번호`에 첫 이미지가 들어갈 문제 번호를 입력합니다.
3. `문제별·부분 캡처 OCR`을 눌러 이미지를 선택합니다. 여러 이미지를 문제 번호 순서대로 한 번에 선택할 수도 있습니다.
4. OCR 결과가 기존의 같은 문제 번호를 대체합니다.
5. 수식, 도형 기호, 선택지 경계는 OCR 오차가 날 수 있으므로 반드시 문제별 편집 화면에서 확인합니다.

권장 캡처 방법:

- 한 이미지에 한 문제만 포함합니다.
- 문제 번호와 모든 선택지를 함께 넣습니다.
- 주변 문제, 머리글, 쪽 번호는 제외합니다.
- 글자가 작으면 150~200%로 확대하고 캡처합니다.
- 기울어진 사진은 바로 세우고 그림자가 없도록 합니다.

이미지 OCR은 처음 사용할 때 브라우저에서 OCR 모듈을 내려받으므로 인터넷 연결이 필요할 수 있습니다.

## 저장 전 최종 점검

- 문항 수가 AMC 25개 또는 수능 30개인지 확인
- 누락 번호가 없는지 확인
- 객관식 선택지가 보통 5개인지 확인
- 선택지 순서와 정답 위치 확인
- 수식의 분수, 지수, 루트, 특수기호 확인
- 도형 문제의 그림과 조건 확인
- 모든 문제의 `검수 완료` 상태 확인

저장된 인터랙티브 세트는 현재 브라우저의 로컬 저장소에 보관됩니다. 브라우저 데이터 삭제나 다른 기기 사용 전에는 원본 파일도 별도로 보관하세요.

