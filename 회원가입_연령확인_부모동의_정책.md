# 회원가입 — 연령 확인 / 보호자 동의 정책

**작성 배경**: 회원가입 폼에 비밀번호·학년·학교유형·국가만 있고 닉네임·비밀번호 찾기·미성년자 동의 절차가 없다는 지적에서 시작된 논의를 정리한다.
**관련 법령**: 개인정보보호법 제22조의2(아동의 개인정보 보호) — 만 14세 미만 아동의 개인정보를 처리하려면 원칙적으로 법정대리인(보호자)의 동의를 받아야 한다. **이 요건은 유료/무료 여부와 무관하게, "만 14세 미만에게서 개인정보를 수집하는가"로 발동한다.**

---

## 서비스 구조와 동의가 필요한 지점

1. **기본 문제 (무료, 비로그인)**: 로그인 없이 제공 — 개인정보 수집 자체가 없으므로 동의 이슈 없음.
2. **기본 문제의 개인 통계/대시보드 (무료, 로그인 필요)**: 현재 이메일·비밀번호·이름·생년월일·학년·국가를 수집하는 자체 회원가입(Phase 1에서 구현). 초등학생도 대상이라 **유료화 여부와 무관하게 만 14세 미만이면 이미 동의 요건이 발동하는 지점**이지만, 아래 "현재 결정"에 따라 지금 당장은 본격적인 동의 플로우를 만들지 않기로 함(잔여 리스크로 인지하고 수용).
3. **입시용 수학 자료 / 경시대회 문제 (유료, 향후 출시)**: 결제가 결부되므로 부모의 실질적 관리·동의가 필요해지는 지점. 이 단계부터 부모 계정/승인 구조를 본격 도입하기로 함.

## 현재 결정 (2026-08-30)

- **지금 구현한 것**: 회원가입 시 생년월일(`birthDate`, 필수)을 받아 `users.birth_date`에 저장 (`migrations/0002_add_birth_date.sql`). 만 14세 미만 여부를 이후 언제든 계산할 수 있도록 데이터만 먼저 확보한다.
- **지금 구현하지 않은 것**: 만 14세 미만 가입을 막거나, 부모 이메일 동의 절차를 요구하거나, 가입을 부모 계정으로 전환하는 로직. 즉 **현재도 만 14세 미만 학생이 이메일/비밀번호만으로 자유롭게 가입할 수 있는 상태이며, 이는 완전한 법적 안전판이 아니라 사용자가 인지하고 수용한 잔여 리스크다.**
- **닉네임/비밀번호 찾기**: 이번 결정과 별개로 아직 미구현 (추후 별도 작업).

## 유료 콘텐츠 출시 시점에 할 일

- 부모 계정 분리형(가입 주체는 성인, 학생은 그 아래 로그인 자격 없는 프로필로 존재) 구조를 본격 설계
- 만 14세 미만으로 계산되는 기존 가입자에 대한 소급 처리 방안 결정 (재동의 요청 등)
- 정식 법률 검토(자문)를 통해 위 구조가 개인정보보호법 요건을 충족하는지 확인 — 이 문서의 정책은 사용자의 실무적 판단이며 법률 자문을 대체하지 않는다.

## 관련 파일

- `migrations/0002_add_birth_date.sql`
- `functions/api/auth/signup.js` (birthDate 필수 검증)
- `functions/api/auth/_shared.js` (`getSessionUser`/`toPublicUser`에 `birthDate` 포함)
- `app/components/AuthModal.js` (생년월일 입력 필드 + 안내 문구)
