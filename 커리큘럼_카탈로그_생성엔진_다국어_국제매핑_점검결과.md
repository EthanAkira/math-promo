# 점검 결과: 커리큘럼 카탈로그 · 문제 생성 엔진 · 다국어 · 국제과정 매핑

**점검일**: 2026-08-29
**방법**: 코드 직접 읽기/grep (추정·설명 아님). 파일 경로와 라인은 실제 근거로 첨부.

---

## 1. 준비 중(pending) 노드 수 확인

**대상 파일**: `app/curriculumCatalog.js`

`availability: 'planned'` 문자열은 이 파일에 총 **25회** 등장. 이 중 2건은 카테고리(그룹) 자체의 상태이고, 나머지 **23건이 개별 단원(topic) 노드**다.

### 카테고리 자체가 '준비 중'인 것 (2개)
- `kr-2022-convergence` (융합 선택)
- `kr-2022-professional` (전문·심화 과목)

### 개별 단원(topic)이 '준비 중'인 것 (23개)

| 소속 | 단원 |
|---|---|
| 중2 | 도형의 닮음과 피타고라스 정리 |
| 중3 | 삼각비 |
| 중3 | 원의 성질 |
| 2022 공통 과목 | 기본수학1 |
| 2022 공통 과목 | 기본수학2 |
| 2022 진로 선택 | 경제 수학 |
| 2022 진로 선택 | 인공지능 수학 |
| 2022 진로 선택 | 직무 수학 |
| 2022 융합 선택 | 수학과 문화 |
| 2022 융합 선택 | 실용 통계 |
| 2022 융합 선택 | 수학과제 탐구 |
| 2022 전문·심화 | 전문 수학 |
| 2022 전문·심화 | 이산 수학 |
| 2022 전문·심화 | 고급 대수 |
| 2022 전문·심화 | 고급 미적분 |
| 2022 전문·심화 | 고급 기하 |
| 국제(Geometry) | Congruence & Similarity |
| 국제(Geometry) | Coordinate Geometry & Proofs |
| 국제(Integrated Math) | Math II · Geometry & Quadratics |
| 국제(Integrated Math) | Math III · Advanced Functions & Modeling |
| 영역별(기하) | 평면도형과 입체도형의 성질 |
| 영역별(기하) | 삼각비와 삼각함수 도형 활용 |
| 영역별(모델링) | 자료 기반 통계 모델링 |

참고로 `availability: 'partial'`(일부 가능) 상태 노드도 다수 존재하는데, 이는 이번 질문 범위 밖이라 별도 집계하지 않음.

---

## 2. 2022 개정 반영 — 카테고리 명칭만인가, 로직도 바뀌었는가

**결론: 카테고리 명칭/설명 텍스트는 2022 개정 용어로 갱신됐지만, 사용자가 예시로 든 4개 세부 토픽(이차함수 최대·최소, 외분점, 원순열, 모비율/표본비율)은 생성 로직에 전혀 구현되어 있지 않다.**

repo 전체에서 `외분점|원순열|모비율|표본비율`을 검색한 결과, 코드(.js)에는 **단 한 곳도 없고**, 계획 문서 `Daily_Learning_Lab_유료화_생성엔진_설계.md`에만 등장한다 — 즉 아이디어 단계이며 미구현.

개별로 확인한 근거:

- **이차함수 최대·최소**: `secondaryAlgebraEngine.js:147-153`의 `quadraticFunctions()`는 오직 "그래프의 꼭짓점 좌표 구하기"만 생성한다. 정의역이 주어졌을 때의 최댓값/최솟값을 구하는 문제 유형은 없음.
- **원순열**: `secondaryAlgebraEngine.js:193-204`의 `permutationsCombinations()`는 순열(nPr)·조합(nCr)만 생성. 원순열(circular permutation, (n-1)!) 로직은 없음.
- **모비율/표본비율**: `koreanElectivesEngine.js`의 통계 관련 함수들(`sampleStatistic`, `confidenceEstimate` 등, 92·104·112행)은 "표본평균"과 "추정 구간(오차한계)"만 다룬다. 모비율/표본비율 관련 함수·문구는 전혀 없음.

**해석**: `curriculumCatalog.js`의 라벨은 "수학Ⅰ → 대수", "확률과 통계" 등 2022 개정 공식 명칭으로 리네이밍되어 있고 `meta: { legacy: ..., revised2022: ... }` 같은 대응 표기도 있지만, 이는 **분류 체계 수준의 매핑**이다. 실제 신규 교육과정에서 강조되거나 새로 들어온 세부 스킬(예시로 든 4개)은 생성 엔진에 반영되지 않은 상태 — 즉 "카테고리 이름만 바뀌고 문제 로직은 구 교육과정 그대로"인 부분이 실제로 존재한다.

---

## 3. 다국어 지원 범위 — 라벨만인가, 문제 본문/해설까지인가

**결론: 15개 언어 딕셔너리는 UI·내비게이션·카테고리 라벨 전용이다. 실제 문제 본문과 해설은 사실상 한국어/영어 2개 언어뿐이며, 나머지 13개 언어는 전부 영어로 대체 표시된다.**

근거:

1. `app/i18n.js`의 `LANGUAGES` 배열: ko, en, en-SG, zh-CN, zh-HK, zh-TW, ja, fr, es, ru, ar, pt, hi, vi, id — 15개. `curriculumCatalog.js`의 `CURRICULUM_COPY`도 동일한 15개 키를 가지지만, 내용은 전부 `eyebrow/title/mainTabs/badges/notices/labels` 등 **탐색 화면 문구**뿐이다.
2. 실제 문제 생성 엔진(`secondaryAlgebraEngine.js`, `algebraCompletionEngine.js`, `koreanElectivesEngine.js` 등, 총 8개 파일 71곳)은 `withEnglish(extra, promptEn, explanation, explanationEn)` 패턴으로 **한국어/영어만 하드코딩**되어 있다.
3. 언어 분기 함수 `isNonKorean(language)`(`app/i18n.js:215`)의 실제 정의는 `return language !== 'ko'` — 즉 한국어가 아니면 전부 "foreign" 취급되어 **영어 텍스트로 통일 표시**된다. 15개 언어를 개별 분기하는 로직 자체가 없다.
4. 예외: `app/regionalCatalog.js`는 초등(`elementary/practice`) **단원 라벨**에 한해 zh-HK/zh-TW/en-SG(싱가포르 철자) 번역을 별도로 제공한다. 하지만 `PracticeGenerator.js:94`를 보면 실제 매 문제의 프롬프트 텍스트는 `isNonKorean(language) && problem.promptEn ? problem.promptEn : problem.prompt`로, zh-HK를 선택해도 문제 본문은 영어로 나간다 — 라벨만 중국어, 본문은 영어인 상태.

즉 다국어 지원의 실제 범위는: **탐색 UI(15개 언어) > 초등 단원 라벨 일부(ko/en/en-SG/zh-HK/zh-TW) > 문제 본문·해설(ko/en 2개뿐, 나머지 13개 언어는 전부 영어로 대체)** 로 3단계 격차가 있다.

---

## 4. 국제과정 매핑 정합성 — 실제 데이터 구조

**결론: Algebra 1/2·Precalculus는 단원(unit) 단위로 실제 태그 매핑이 존재하지만, Geometry는 그런 매핑 없이 카테고리 껍데기만 있다.**

`PRE_ALGEBRA_PROFILES` (`catalog.js:189-203`) 정의:

| profile id | 라벨 |
|---|---|
| kr-middle-1 / 2 / 3 | 한국 중1·중2·중3 |
| kr-high-1 | 한국 고1 · 공통수학 |
| kr-high-2-algebra / calculus-1 / probability-statistics | 한국 고2 (대수/미적분Ⅰ/확통) |
| kr-high-3-calculus-2 / geometry | 한국 고3 (미적분Ⅱ/기하) |
| pre-algebra, algebra-1, algebra-2, precalculus | 국제(미국계) 과정 |

이 profile들은 각 생성 엔진 파일에서 실제 **단원(unit) 배열에 태그**로 연결된다. 예:

```js
// secondaryAlgebraEngine.js:275-283
const P = { M2:['kr-middle-2'], M3:['kr-middle-3'], H1:['kr-high-1'],
  H2A:['kr-high-2-algebra'], H2S:['kr-high-2-probability-statistics'],
  A1:['algebra-1'], A2:['algebra-2'], PC:['precalculus'] };
```

예시 (같은 unit이 한국 학년과 국제 과정에 동시 태깅됨):
- `지수법칙`, `식의 계산`, `일차부등식`, `연립일차방정식`, `일차함수`, `경우의 수와 확률` → `profiles(P.M2, P.A1)` = 한국 중2 ↔ 국제 Algebra 1
- `완전제곱식`, `판별식과 근의 종류` → `profiles(P.H1, P.A1, P.A2)` = 한국 고1 공통수학 ↔ Algebra 1·2 동시
- `다항식의 나눗셈`, `복소수 이차방정식`, `직선과 이차함수의 교점` → `profiles(P.H1, P.A2, P.PC)` = 한국 고1 ↔ Algebra 2·Precalculus

이 매핑 자체는 내용상 크게 부자연스럽지 않다(한국 중2 시점 학습 내용과 미국 Algebra 1 표준 내용이 실제로 겹치는 편). 다만 **공식 커리큘럼 대조표(concordance)에 근거한 것이 아니라 개발자가 임의로 부여한 태그**이며, 이를 검증할 출처 문서는 repo 내에 없다.

**결정적 격차**: Geometry는 이 태그 시스템에 아예 포함되어 있지 않다.
- `curriculumCatalog.js`의 `intl-geometry` 스테이지는 `/middle-school/basic-figures`로 연결되는데, `app/middle-school/basic-figures/catalog.js`(293줄) 전체를 검색해도 `profile` 문자열이 **0건**이다.
- 즉 Algebra 1/2/Precalculus는 "어떤 한국 학년 단원과 대응되는가"가 unit 단위로 세밀하게 태깅되어 있지만, Geometry는 국제/한국 구분 없이 **단일 페이지 하나**를 공유할 뿐 실제 매핑 데이터가 없다. "카테고리 존재 = 정합성"이 아니라는 우려가 Geometry에서는 실제로 맞는 상황이다.

---

## 후속 조치 결과 (2026-08-29)

2번(2022 개정 세부 토픽)과 4번(Geometry 국제과정 매핑)을 실제로 해결했다. 나머지 1번(준비 중 23개 단원 신규 구현)·3번(13개 언어로 문제 본문 확장)은 별도 프로젝트 규모라 이번 범위에서 제외했다(사용자 확인).

### 2번 — 2022 개정 4개 세부 토픽 구현
| 토픽 | 위치 | 내용 |
|---|---|---|
| 이차함수의 최대·최소 | `secondaryAlgebraEngine.js`의 `quadraticMaxMin()`, unit id `quadratic-max-min` | 주어진 폐구간에서 꼭짓점/끝점 중 최댓값·최솟값을 판별 |
| 원순열 | `secondaryAlgebraEngine.js`의 `circularPermutations()`, unit id `circular-permutations` | (n−1)! 공식으로 원탁 배열 수 계산 |
| 외분점 | `algebraCompletionEngine.js`의 `externalDivision()`, unit id `external-division-coordinate` | 외분 공식 P=((−n·A+m·B)/(m−n))으로 좌표 계산, 기존 내분점과 동일 스타일 |
| 모비율·표본비율 | `koreanHigh2Engine.js`의 `sampleProportion()`, unit id `h2-sample-proportion` | 표본비율 p̂=X/n 계산 + 모비율 신뢰구간(오차한계 방식) 추정 |

모두 각 파일의 기존 `profiles`/`unit()` 관례를 그대로 따라 등록했고, `npx next build`로 전체 31개 페이지 정적 생성이 문제없이 통과하는 것을 확인했다.

### 4번 — Geometry 국제과정 매핑
점검 중 예상과 다른 사실을 발견했다: **Geometry에도 이미 프로필 필터링 시스템(`geometryProfiles.js`, `GEOMETRY_PROFILES`)이 존재**했다. 다만 Algebra 계열의 `kr-middle-2`/`algebra-1` 같은 학년/코스 네임스페이스가 아니라 `kr`/`g12`/`ib`/`amc12`/`csat` 같은 **시험·트랙 단위 네임스페이스**였고, `curriculumCatalog.js`의 `intl-geometry`(국제 Geometry 단계)는 이 시스템에 아예 연결되지 않은 채 `planned`로 방치되어 있었다.

확인해보니 "Congruence & Similarity"와 "Coordinate Geometry & Proofs"는 이미 `triangle-congruence-similarity`, `high-coordinate-geometry` 유닛으로 실제 구현되어 있었다 — 카탈로그 링크만 빠져 있었던 것. 아래와 같이 연결했다:

- `intl-geometry` 스테이지: Congruence & Similarity → `/middle-school/basic-figures?unit=triangle-congruence-similarity` (ready)
- `intl-geometry` 스테이지: Coordinate Geometry & Proofs → `/middle-school/basic-figures?unit=high-coordinate-geometry` (ready), 스테이지 전체 상태도 `partial→ready`로 갱신
- `domain-geometry`(영역별 보기)의 "삼각비와 삼각함수 도형 활용" → 기존 `radians-trig-ratios` 유닛(수능 기하 프로필)에 연결, `/middle-school/basic-figures?unit=radians-trig-ratios&profile=csat`

**남은 진짜 격차**: `CORE_GEOMETRY_UNITS`/`MIDDLE_GEOMETRY_BASIC_UNITS`(기초·중학 기하, 약 20개 유닛)에는 여전히 Algebra 수준의 학년별 profile 태그가 전혀 없어 모든 프로필에 동일하게 노출된다. 이걸 Algebra와 동일한 세밀도로 태깅하려면 유닛 하나하나의 실제 난이도·교육과정 위치를 판단해야 하는 커리큘럼 설계 작업이라 이번 세션 범위에서 제외했다. `domain-geometry`의 "평면도형과 입체도형의 성질"도 정확히 매칭되는 단일 유닛이 없어 `planned`로 남겨뒀다(어설픈 연결보다 정직하게 미해결로 표시).

---

## 종합 미흡점 요약

1. `curriculumCatalog.js` 노드 23개(+ 카테고리 2개)가 '준비 중' 상태로 실제 학습 불가.
2. 2022 개정 세부 신규 토픽(이차함수 최대·최소, 원순열, 모비율/표본비율, 외분점 등)은 생성 엔진에 미구현 — 라벨만 개정, 로직은 구 교육과정.
3. 15개 언어 중 실질적으로 문제 본문·해설이 지원되는 언어는 한국어·영어 2개뿐. 13개 언어는 UI만 현지어이고 문제는 영어로 노출됨(사용자 기대와 괴리 가능).
4. 국제과정 매핑은 Algebra 1/2/Precalculus에는 실재하지만 Geometry에는 없음 — 국제학교 학부모 대상 마케팅 시 Geometry 항목은 "존재는 하지만 실제 매핑 근거가 없는" 상태임을 인지 필요.
